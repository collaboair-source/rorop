# AI 비서 (없으면 비서 기능만 비활성화)
ANTHROPIC_API_KEY=
# HQ_AI_ENABLED=1   # API 키 환경변수 없이 SDK 프로필 인증을 쓸 때
# SECRETARY_MODEL=claude-opus-5

# 로그인 토큰 서명 키 — 운영에서는 반드시 변경
JWT_SECRET=change-me

# 데이터 저장 위치 (기본 ./data)
# RORO_DATA_DIR=./data

# 서버 시간대 (브라우저 시간대 쿠키가 우선 적용됨)
# TZ=Asia/Seoul
